import 'package:flutter/material.dart';
import 'package:help_center/widgets/widgets.dart';

class AjustesPacienteScreen extends StatelessWidget {
  const AjustesPacienteScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Ajustes'),
    );
  }
}